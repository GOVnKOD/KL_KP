encoding "utf8";

TTextMinerConfig {
  Dictionary = "dic.gzt";

  PrettyOutput = "pretty.html";

  Input = {
    File = "input.txt";
  }

  Articles = [
    { Name = "People_Places" }
  ]

  Facts = [
  { Name = "Fact"  }
  ]

  Output = {
    File = "output.txt";
    Format = text;
  }
}